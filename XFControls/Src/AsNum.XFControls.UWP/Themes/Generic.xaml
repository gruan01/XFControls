<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AsNum.XFControls.UWP">

    <Style TargetType="local:DataPicker">
        <Setter Property="Height" Value="150" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <!--<Setter Property="DividerColor" Value="Gray" />-->
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:DataPicker">
                    <Grid>
                        <ScrollViewer x:Name="sc" 
                                        Padding="0" 
                                        HorizontalScrollMode="Disabled" 
                                        VerticalScrollBarVisibility="Hidden">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="50" x:Name="r1" />
                                    <RowDefinition Height="auto" />
                                    <RowDefinition Height="50" x:Name="r3" />
                                </Grid.RowDefinitions>
                                <ItemsPresenter Grid.Row="1" />
                            </Grid>
                        </ScrollViewer>
                        <Border BorderBrush="{TemplateBinding DividerColor}" 
                                            BorderThickness="0,1,0,1" 
                                            VerticalAlignment="Center"
                                            Height="40" />
                    </Grid>

                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Grid Height="40">
                        <TextBlock Text="{Binding}"
                                   VerticalAlignment="Center" 
                                   HorizontalAlignment="Center"
                                   Foreground="Red"
                                   />
                    </Grid>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>







    <Style TargetType="local:Control4">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:Control4">
                    <Grid>
                        <TextBlock x:Name="txt" 
                                   Text="{TemplateBinding Text}"
                                   Foreground="{TemplateBinding TextColor}"
                                   />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
